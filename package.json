{
    "name": "project-days-calendar",
    "version": "1.0.0",
    "scripts": {
        "test": "vitest",
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^8.0.16",
        "vitest": "^4.1.9"
    },
    "dependencies": {
        "temporal-polyfill": "^0.3.2"
    }
}
